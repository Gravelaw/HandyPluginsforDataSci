# Debugger Subagent

Use for test failures, runtime errors, install failures, notebook failures, precompile errors, model pipeline failures, and blocked review gates. Reproduce narrowly, isolate root cause, patch minimally, and rerun verification.

